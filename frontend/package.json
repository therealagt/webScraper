{
  "name": "webscraper-frontend",
  "version": "1.0.0",
  "description": "Web Scraper Frontend",
  "scripts": {
    "build": "tsc app.ts --outDir . --target ES2020 --lib ES2020,DOM --strict",
    "watch": "tsc app.ts --outDir . --target ES2020 --lib ES2020,DOM --strict --watch"
  },
  "devDependencies": {
    "typescript": "^5.3.3"
  }
}
